<nav class="navbar">
    <div class="navbar-items">
        <a href="#" onclick="toggleCard('dragonStatsCard')">Dragon Stats</a>
        <a href="#" onclick="toggleCard('gardenStatsCard')">Garden Stats</a>
        <a href="#" onclick="toggleCard('settingsCard')">Settings</a>
        <a href="#" onclick="toggleCard('chatCard')">Chat</a>
    </div>
    <div class="navbar-right">
        <span id="lavaJuiceCount">Lava Juice: 100</span>
        <button onclick="toggleTheme()">Toggle Theme</button>
        <% if (session && session.userId) { %>
            <button onclick="window.location.href='/auth/logout'">Logout</button>
        <% } else { %>
            <button onclick="window.location.href='/auth/login'">Login</button>
        <% } %>
    </div>
</nav>